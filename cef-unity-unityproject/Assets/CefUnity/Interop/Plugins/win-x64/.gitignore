# CEF runtime + Rust binaries are regenerated by deploy.ps1 (cargo build).
# Only cef_unity_rust.dll (Unity's DllImport target) and Unity .meta files are tracked.
*
!.gitignore
!*.meta
!cef_unity_rust.dll
